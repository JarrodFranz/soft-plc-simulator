# Multi-Phase Roadmap — Mobile Soft PLC Simulator

> **Warning**: This document outlines planned phases for the Mobile Soft PLC Simulator. All phases prioritize software simulation, SCADA integration, and virtual commissioning rather than hardware safety certification.

---

## Phase 0: Project Scaffold, Shared Types & Documentation
- **Objective**: Establish codebase architecture, core schemas, unit test suite, documentation, and continuous integration baseline.
- **Deliverables**:
  - `README.md`, `PROJECT_BRIEF.md`, `ARCHITECTURE.md`, `DECISIONS.md`, `DEVELOPMENT_RULES.md`, `SECURITY_AND_SAFETY.md`.
  - JSON schemas for project files, tags, runtime configs, and protocol maps in `shared/schemas/`.
  - Example project JSON files in `examples/projects/`.
  - Rust workspace setup with `soft-plc-runtime` crate and companion `gateway`.
  - Flutter workspace scaffold targeting Web, Desktop, Android, and iOS.
- **Status**: ✅ **COMPLETED**

---

## Phase 1: Core Tag Database, Deterministic Scan Engine & Simulated I/O Image
- **Objective**: Build the core memory image, tag database with forced value overrides, scan cycle metric engine, and basic ISA runner.
- **Deliverables**:
  - `TagDatabase` supporting `BOOL`, `INT16/32/64`, `FLOAT32/64`, `STRING`, quality flags, timestamps, and force matrix overrides (`runtime/src/tag.rs`).
  - `ScanEngine` executing deterministic scan cycles with timing metrics (min/max/avg scan time, overrun counts) (`runtime/src/scan_engine.rs`).
  - Ladder Logic Instruction Set Architecture (`XIC`, `XIO`, `OTE`, `OTL`, `OTU`, parallel branch OR frames, `TON`/`TOF` timers) (`runtime/src/instructions.rs`).
  - 43 Rust unit tests verifying tag behavior, forcing, scan metrics, and instruction logic.
- **Status**: ✅ **COMPLETED**

---

## Phase 2: Structured Text (ST) Engine, ST Autocomplete IDE, Memory Manager & Grid HMI Builder
- **Objective**: Full Structured Text (ST) language support with recursive-descent parser, AST interpreter, live autocomplete IDE, Memory Manager (DUT/DB), and custom Grid HMI Dashboard Builder.
- **Deliverables**:
  - ST Lexer, Parser, AST Compiler, and Interpreter (`runtime/src/st/`).
  - **Live Autocomplete ST Editor**: Code completion overlay for global tags, Data Blocks, Structs, built-in function blocks (`TON`, `TOF`, `TP`, `CTU`, `CTD`), math utilities (`LIMIT`, `SEL`, `ABS`, `SQRT`), and keywords.
  - **Memory & Data Storage Manager**: Global Tags, Struct Definitions (DUT / User Defined Types), and Data Blocks (DB).
  - **Custom Grid HMI Dashboard Builder**: Interactive `RUN MODE` vs `EDIT BUILDER` mode with tag-linked input, output, and display components (`PushbuttonSwitch`, `ToggleSwitch`, `NumericSliderInput`, `TextInputField`, `LedIndicatorLight`, `DigitalGaugeDisplay`, `StatusPillDisplay`, `TankGraphicDisplay`) supporting direct Drag-and-Place positioning and grid snap width resizers.
- **Status**: ✅ **COMPLETED**

---

## Phase 3: Complete IEC 61131-3 Language Editors & Autocomplete Suite (ST, LD, FBD, SFC)
- **Objective**: Full graphical and textual IDE support for ALL 4 IEC 61131-3 programming languages with dedicated autocomplete palettes.
- **Deliverables**:
  - **Structured Text (ST)**: Textual IDE with live autocomplete suggestions, templates, and AST verification.
  - **Ladder Logic (LD)**: Node-and-wire graph rung editor with a continuous `L1`/`L2` power-rail frame, contacts (NO/NC/rising/falling), coils (coil/negated/set/reset) pinned terminal against the right rail, parallel (OR) branches over any element span with draggable snap endpoints, and `TON`/`TOF` timer blocks.
  - **Function Block Diagram (FBD)**: Graphical Signal Flow Diagram Editor with drag-and-drop block placement (`AND`, `OR`, `NOT`, `TON`, `LIMIT`, `TAG_INPUT`, `TAG_OUTPUT`), signal wires, and FBD block autocomplete palette.
  - **Sequential Function Chart (SFC)**: State Machine Chart Editor with Initial Steps, Steps, Transitions, ST Actions, and Condition Autocomplete Palette.
- **Status**: ✅ **COMPLETED**

**LD visual rework + Go-Online live monitor (post-ship)** ✅ — branch risers
now centre in the inter-cell gap next to the branch element they serve
(`kLdGapHalf`/`ldRiserXBefore`/`ldRiserXAfter` in `ld_layout.dart`) instead of
sitting flush against a cell edge, and contact/coil symbols are laid out on a
`Stack` centred on the wire with the tag name captioned above. A new
session-only **Go-Online** toggle (app-bar `Icons.sensors`) overlays the last
scan's power-flow solve on the rung canvas via a transient `LdMonitor` tap
in `executeRung`: energized wires/elements glow bright green, de-energized
dim to slate, and block faces show live ACC/PT, CV/PV, and resolved
compare/math operands. Repaints via the existing `LiveTick` pattern (no
shell rebuild); gated on the toggle alone so a paused scan **freezes** the
last live state instead of going blank. Nothing is persisted — guarded by a
dedicated round-trip test asserting no `nodePower`/`online` key ever reaches
serialized project JSON. Documented in [`docs/ld-editor.md`](docs/ld-editor.md).

**SFC alternative branching (post-ship)** ✅ — a step in the SFC editor can
now own multiple ordered outgoing transitions, each with its own editable
target step and condition; top-to-bottom order is if/else-if priority
(single-token OR/selection branching, not parallel/AND — the engine picks
the first true outgoing transition on the one active step, same as
before). The step card gained an **Add branch** button and, per outgoing,
a target dropdown (every step plus **"＋ New step…"**), reorder-priority
controls, and a delete-branch control (`mobile/lib/screens/sfc_editor_screen.dart`,
`mobile/lib/models/sfc_edit.dart`). Steps now lay out in **flow order**
from the initial step via a new pure helper (`mobile/lib/models/sfc_layout.dart`);
a non-inline outgoing renders as a GOTO chip — a loop-back icon for a
target at/above, a forward-branch icon for a target further down, or
`(deleted)` for a dangling target. Deleting a step removes every
transition referencing it in either direction and promotes a new initial
step if needed. The engine (`sfc_exec.dart`) was **not changed** — it
already implemented first-true-wins priority over a single active token —
and nothing new is persisted, since the step/transition graph already
round-tripped arbitrary branch counts. Documented in
[`docs/sfc-branching.md`](docs/sfc-branching.md).

**SFC v2: 2D layout + parallel (fork/join) branching (post-ship)** ✅ — the
SFC editor now draws a real textbook-style 2D chart (step boxes and
transition **blocks** as distinct elements, alternative branches laid out
as side-by-side columns, parallel fork/join as double-line bars, nested to
any depth, with GOTO reference chips for loop-backs and reconvergence)
instead of the old single flow-ordered list. `SfcTransition` gained three
additive fields — `kind` (`'single'` / `'parallelFork'` / `'parallelJoin'`),
`toStepIds`, `fromStepIds` — and `SfcRuntime.active` became a per-program
**set** of simultaneously-active step ids instead of one token: a fork
activates every branch head at once, a join waits until every branch tail
is active before firing, and an alternative divergence keeps its original
first-true-by-list-order priority. A pure parser (`sfc_region.dart`) turns
the step/transition graph into a region tree and a pure layout pass
(`sfc_layout2.dart`) turns that into absolute 2D geometry for the new
pan/zoom canvas; structured-authoring helpers (`sfc_edit.dart`) add/nest/
collapse alternative and parallel branches from the step menu without ever
producing a dangling reference. Go-Online highlighting is parallel-aware
(every step in the live active set glows, so both branches of an active
fork light up together) and remains session-only — a project's serialized
JSON is provably identical whether or not it was ever taken online. Fully
additive and backward-compatible: a pre-v2 project file (all `single`
transitions, none of the new keys) loads and round-trips unchanged. The
old flow-ordered list layout (`sfc_layout.dart`) has been retired now that
the 2D pipeline fully supersedes it. Documented in
[`docs/sfc-branching.md`](docs/sfc-branching.md).

**SFC v2 Minors + Batch Mix & Dispatch showcase (post-ship)** ✅ — the two
deferred v2 Minors are fixed (`addParallelBranch` now guards against
forking from an alternative-divergence head, and deleting a fork-source
step removes the whole fork/join construct instead of leaving a dangling
reference), the per-column parallel join connector and internal
`_stepKey`/`_guardFrag` helpers were de-duplicated, and a new default
project, **SFC — Batch Mix & Dispatch**, ships as a from-scratch showcase
of both branch shapes together (parallel Heat + Fill fork/join, then an
alternative `Quality_OK` dispatch/reject gate). See
[`docs/sfc-branching.md`](docs/sfc-branching.md).

---

## Phase 3.5: Structured Tag System, Simulated I/O & In-App Execution Engines ✅
- **Objective**: Make the in-app simulator real — structured tags resolved by path, data-driven input simulation, and actual execution of the programs built in the editors (in-app Dart engines; see ADR-009). **All four IEC 61131-3 languages now execute; every default project's outputs are driven by real engines, and the previously hardcoded per-project logic is fully retired.**
- **Deliverables**:
  - **Structured tag & type system**: `PlcTag` values as real trees (struct `Map`s, array `List`s); DUT-typed tags replace the retired Data Blocks concept; built-in `TIMER` composite; a pure path resolver (`readPath`/`writePath`/`childrenOf`) for members (`TONTimer.DN`), integer bits (`Word.5`), and array elements (`Recipe[3]`); recursive Memory Manager expansion; path-aware scan accessors and editor tag pickers. ✅
  - **Simulated I/O rules engine**: editable, condition-gated input behaviours (`pulse`, `ramp`, `integrate`, `delayedSet`, `setWhileCondition`) with per-second rates and a dedicated editor screen; the previously hardcoded per-project input physics migrated into visible default rules. ✅
  - **Ladder execution engine**: pure power-flow interpreter over the LD graph (series=AND, parallel=OR, evaluated in topological column order), latch/edge/pulse coil semantics, `TON`/`TOF` timers counting live in their `TIMER` struct tags (scan-tick clock), forcing always wins; hardcoded LD control logic replaced for the motor, conveyor, and water-pump projects; verified by end-to-end scan tests. ✅
  - **SFC execution engine**: pure ST-subset expression/assignment evaluator (`st_expr.dart` — the seed of the ST interpreter) runs step actions and transition conditions with an implicit `STEP_T` step timer; `sfc_exec.dart` drives one active step per program with N-action semantics, first-true-transition switching, and force-aware writes; the bottle filler and water-plant backwash migrated from hardcoded state machines to executed charts. ✅
  - **FBD execution engine**: pure topological dataflow evaluator (`fbd_exec.dart`) over the `FbdBlock`/`FbdWire` graph with a full block set (AND/OR/NOT, ADD/SUB/MUL/DIV, comparators GT/LT/GE/LE/EQ/NE, IEC `LIMIT` clamp, `CONST`, TAG_INPUT/TAG_OUTPUT), input order = wire order, never-throws/never-hangs; the HVAC zone controller and water-quality gate migrated from hardcoded logic to executed diagrams (with editor palette + `CONST` literal editing). ✅
  - **ST interpreter**: pure statement interpreter (`st_exec.dart`) — `IF`/`ELSIF`/`ELSE` (nested) + assignments — built on the `st_expr` expression core; tokenizes with source offsets and evaluates each expression as an exact source substring; force-aware writes; never throws and never hangs (parser progress guaranteed on any input). The reactor deadband controller runs as `ReactorTemp_ST` and water alarms/permissive as a trimmed `Safety_ST`; a single authoritative owner was assigned per tag (`Quality_OK`→FBD, `Treat_Dosing`→LD, tank→FBD) and the last hardcoded logic (`_evaluateActiveLogic`) was retired. ✅
- **Status**: ✅ **COMPLETED — LD, SFC, FBD & ST execution shipped; every default project runs entirely through a real engine (no hardcoded logic remains)**

---

## Phase 3.6: Project Persistence & Portability ✅
- **Objective**: Make projects survive restarts and move between devices — one cross-platform storage path for the single app targeting the iOS/Android stores and desktop.
- **Deliverables**:
  - **Lossless serialization** of the whole `PlcProject` graph (LD rungs, FBD blocks/wires, SFC steps/transitions, struct defs, and the structured tag value tree + forcing were previously dropped on save) — proven by a 20-scan scan-equivalence round-trip per default project. ✅
  - **`ProjectRepository`** over `shared_preferences` (universal backend — Android/iOS/desktop/web, one code path): catalog + defensive reads, seed-defaults-on-first-run, and full project CRUD. ✅
  - **Shell integration**: boot from the repository with the last active project restored; debounced autosave with a Saving/Saved/Save-failed (and a visible "not saved — storage unavailable") indicator; New/Duplicate/Rename/Delete/Reset in the project switcher. Boot uses try/catch channel detection (no timeout race), so a slow-but-working device always persists. ✅
  - **Export/Import** of `.splc.json` files (`share_plus`/`file_picker`) for cross-device transfer with no cloud; crash-proof import (malformed files raise a typed error, never crash) and id-collision reassignment. ✅
  - **Non-destructive default-project backfill (post-ship)** ✅ — `seedDefaultsIfEmpty` only ever seeded when the catalog was *empty* (first launch), so a default project added in a later release never reached an existing install. `backfillNewDefaults` (`ProjectRepository`) now runs on every boot and adds only defaults whose id has never been seeded on this device, tracked in a persisted `seeded_default_ids` ledger (bootstrapped from the catalog on a pre-migration install, so nothing already present is duplicated or overwritten). User edits to a default are never clobbered, and a default the user deliberately deleted **stays deleted** — the ledger, not the catalog, is authoritative for "seeded". `resetToDefaults` seeds via the same path so the ledger is rebuilt immediately (closing a window where a post-reset deletion could be resurrected on the next launch). Additive: one new prefs key, no project-blob or catalog-format change.
- **Status**: ✅ **COMPLETED**

---

## Phase 4: Industrial OPC UA Server Adapter ✅
- **Objective**: The **single app itself hosts** an OPC UA server exposing the user-chosen tags as standard OPC UA variable nodes to SCADA/any OPC UA client (**ADR-010** — no companion service; pure-Dart in-app server; mobile-first, desktop secondary).
- **Direction change (ADR-010)**: the earlier companion-gateway route (WS16–18: a separate Rust process bridged over WebSocket — built, machine-tested, and client-verified) was **retired** when the goal was clarified to "one app hosts everything." Its lasting artifacts: the per-project **Outbound Protocols configuration** ✅ (`ProtocolSettings` with per-protocol enable + config, additive persistence) which carries over unchanged, and the **Rust `opcua` client E2E harness** ✅ (proven against the gateway; branch `feat/opcua-hardening`) which becomes the third-party verifier for the in-app server.
- **Deliverables**:
  - **In-app pure-Dart OPC UA server v1** ✅ — a hand-rolled `opc.tcp` binary server implemented in Dart *inside the app* (no FFI, no companion process): Security `None` + anonymous, Hello/Ack, secure channel (incl. token renewal), sessions, GetEndpoints, and Browse/Read/Write over the per-project exposed-tag map — reading the live tag DB in-process and writing force-aware (an external write to a *forced* tag is refused with `Bad_UserAccessDenied`). `dart:io` is confined to the socket host (`services/opcua_host.dart`); hosting is opt-in via the Outbound Protocols section (Start/Stop, configurable port, default 4840) and is stopped on every project switch. Every wire encoding / service NodeId / status code was verified byte-for-byte against the vendored Rust `opcua` 0.12 reference; the codec is dart2js-compile-safe. **Machine-verified end-to-end** by `tool/opcua_e2e.sh`: a real Rust `opcua` client connects to the in-app Dart server and completes GetEndpoints → Browse → Read → Write → read-back (exact value). Runs on Android / desktop / iOS (iOS serves while the app is foreground; web compiles but cannot host inbound sockets).
  - **Subscriptions/MonitoredItems (live SCADA monitoring) — v2** ✅ — all nine subscription services (`CreateSubscription`/`ModifySubscription`/`DeleteSubscriptions`/`SetPublishingMode`, `CreateMonitoredItems`/`ModifyMonitoredItems`/`DeleteMonitoredItems`, `Publish`/`Republish`; `SetMonitoringMode` deliberately not implemented — mode is set at item create/modify): data-change monitored items on the Value attribute with absolute-deadband change detection, per-subscription keep-alive/lifetime timing driven off a host clock tick, `Republish` retransmission of missed notifications, and fixed caps (10 subscriptions/session, 500 monitored items/subscription, 10 parked publishes/session, 20 retransmission messages/subscription). The Outbound Protocols card surfaces `Subscriptions: N · Monitored items: M` once a client subscribes. **Machine-verified end-to-end** by the extended `tool/opcua_e2e.sh`: after the v1 GetEndpoints/Browse/Read/Write/read-back proof, the real Rust `opcua` client creates a subscription + monitored item and receives a genuine pushed `DataChangeNotification` triggered by a server-side mutation on an independent timer — proof a third-party client is receiving *pushed* data changes, not just polling. See `docs/protocols/opcua.md`'s "Subscriptions (v2)" section for the documented v2 simplifications (Sampling≈Reporting, `TimestampsToReturn` ignored, keep-alives continue while publishing is disabled).
  - **Security — Basic256Sha256 (Sign / SignAndEncrypt) + username/password auth — v3** ✅ — a hand-rolled, pure-Dart OPC UA security stack (no crypto FFI): RSA-2048 + AES-256-CBC + SHA-256/HMAC-SHA256 + RSA-OAEP + RSA-PKCS#1-v1.5-SHA256 + the OPC UA `P_SHA256` key-derivation, a self-signed X.509 application-instance certificate generated + persisted once on first run (`services/opcua_cert_store.dart`, the only security file allowed `dart:io`/`path_provider`; the private key never leaves the device and is never serialized into project JSON), the asymmetric `OpenSecureChannel` (OPN: OAEP-encrypted + PKCS#1-signed) and symmetric `MSG`/`CLO` channel (AES-256 + HMAC-SHA256, keys from `P_SHA256`), multi-policy endpoint advertisement (`None` alongside `Basic256Sha256/Sign` and `/SignAndEncrypt`), Anonymous **and** `UserNameIdentityToken` auth (password OAEP-decrypted + server-nonce-verified), and the UI toggles/credentials/cert-thumbprint/regenerate controls on the Outbound Protocols card. `CreateSessionResponse` now returns the server certificate + server nonce on a secured channel (required by strict clients — found and fixed via the live E2E below). **Machine-verified end-to-end** by the extended `tool/opcua_e2e.sh`: after the None/Anonymous v1+v2 proof, a **real Rust `opcua` client** opens a second session at `Basic256Sha256`/`SignAndEncrypt` authenticated with a username/password and completes Browse → Read → Write → read-back over the fully encrypted channel (`OPCUA SECURITY PROBE PASS`). Documented v3 limitations (see `docs/protocols/opcua.md`): a single app-wide cert is reused across projects (applicationUri must be app-wide-stable), the `ActivateSession` signature check reuses the `CreateSession` server nonce rather than rotating a fresh one per activation, and deprecated policies / X.509 user tokens / a managed trust-list remain deferred.
  - **Strict-client conformance gaps — CLOSED** ✅ — the three gaps the v3 security work above deliberately deferred are now fixed: (1) the self-signed app certificate carries `KeyUsage` (critical: digitalSignature/nonRepudiation/keyEncipherment/dataEncipherment) + `ExtendedKeyUsage` (serverAuth/clientAuth) extensions; (2) `CreateSessionResponse.serverSignature` is populated (RSA-PKCS#1-v1.5-SHA256 over clientCert‖clientNonce); (3) `ActivateSession`'s client signature is verified (RSA-PKCS#1-v1.5-SHA256 over serverCert‖serverNonce; invalid/missing → `Bad_ApplicationSignatureInvalid`). The live Rust `opcua`-client E2E (`tool/opcua_e2e.sh`) is the regression guard for all three: the client's genuine ActivateSession signature must pass server-side verification for `OPCUA SECURITY PROBE PASS` to print, and it does. **Operational note:** existing on-device certs generated before this change lack the new extensions and must be regenerated (OPC UA card → Regenerate, or a fresh install) before testing against a strict client.
  - Real strict-client interoperability — **CONFIRMED ✅** against **Ignition 8.3 (Eclipse Milo stack)**: a full `Basic256Sha256` / `SignAndEncrypt` session with username/password authentication connects, browses, reads, and writes — running perfectly. Reaching it surfaced three real strict-client requirements the lenient Rust probe cannot (each fixed and confirmed live): (a) the self-signed app cert needs **`keyCertSign`** in KeyUsage (Milo validates a self-signed cert as its own trust anchor and rejects an anchor without it — `Bad_CertificateUseNotAllowed`; fixed 2026-07-11); (b) an Ignition-in-Docker client must reach the host via `opc.tcp://host.docker.internal:4840`, not the container's own `localhost`; (c) the **UserName UserTokenPolicy is advertised only when a credential with a non-blank password is loaded** — because passwords are in-memory-only, a password must be re-entered and the OPC UA host Stopped/Started after any app restart, else the client reports "no UserTokenPolicy with UserTokenType.UserName found".
- **Status**: ✅ **SHIPPED & STRICT-CLIENT-CONFIRMED — in-app pure-Dart OPC UA server v1 + v2 subscriptions + v3 Basic256Sha256 security & user auth, machine-verified by the live Rust `opcua` E2E AND confirmed running end-to-end against a real strict client (Ignition 8.3 / Eclipse Milo: Basic256Sha256 SignAndEncrypt + username/password — browse/read/write) (ADR-010; single app hosts everything, no companion service)**

---

## Phase 5: Modbus TCP Server Adapter ✅
- **Objective**: The **single app itself hosts** a Modbus TCP server mapping tags to Coils, Discrete Inputs, Holding Registers, and Input Registers (same "one app hosts everything" model as Phase 4's OPC UA server, ADR-010).
- **Deliverables**:
  - **`ModbusMap`/`ModbusProtocolConfig`** ✅ — a per-project, additive `protocols.modbus` config (enable flag, port, default `502`) with a tag<->address map across the four classic Modbus data tables, editable in the Outbound Protocols section's Modbus TCP card or auto-generated from the project's scalar tags (**Regenerate** — read/write `ioType`s default `ReadWrite`, `SimulatedOutput` defaults `ReadOnly`).
  - **Pure-Dart MBAP/PDU codec + register-file handler** ✅ (`mobile/lib/protocols/modbus/modbus_pdu.dart`, zero Flutter dependency, dart2js-safe): all 8 classic function codes (`01`/`02`/`03`/`04`/`05`/`06`/`0F`/`10`), big-endian wire encoding with high-word-first `INT32`/`FLOAT64` packing, LSB-first coil bit packing, reads that never fail on unmapped gaps (0-fill), force-aware writes (a write to a forced tag is silently discarded but still answers success — deliberately different from OPC UA's visible `Bad_UserAccessDenied` refusal, since the classic Modbus wire has no equivalent rich status), and proper exception responses (`01`/`02`/`03`) on illegal function/address/value.
  - **In-app `dart:io` socket host** ✅ (`mobile/lib/services/modbus_host.dart`, the only file in the project allowed to import `dart:io` for Modbus) — MBAP-length-prefixed frame reassembly over a real `ServerSocket`, hostile/malformed-frame connection isolation (never takes the whole host down), opt-in Start/Stop lifecycle wired into the Outbound Protocols Modbus TCP card (status, mapped-tag count, connected-client count, endpoint display).
  - **Machine-verified end-to-end** by `tool/modbus_e2e.sh`: a real Rust `tokio-modbus` crate client connects to the in-app Dart server and completes a poll-for-server-side-mutation (`read_holding_registers`, proving live reads not a frozen snapshot) → `write_single_register` + independent read-back → `write_single_coil` + independent read-back (`read_coils`). Runs on Android / desktop / iOS (iOS serves while the app is foreground; web compiles but cannot host inbound sockets).
  - Configurable address mapping, persisted per-project (additive `protocols.modbus.map`). ✅
- **Status**: ✅ **SHIPPED — in-app pure-Dart Modbus TCP server v1, machine-verified by a real Rust `tokio-modbus` client (see `docs/protocols/modbus.md`)**

**Interop hardening (post-ship)** ✅ — a live third-party SCADA client and a live Modbus master both surfaced real interoperability bugs the automated probes hadn't caught: the OPC UA server's forced-write refusal didn't cover the general read path (fixed: forcing now flows through the shared `readPath` resolver everywhere, including reads), the address space didn't answer the standard `NamespaceArray`/browse-from-Root discovery sequence strict clients use before addressing anything (fixed — see `docs/protocols/opcua.md`), and the Modbus register map only supported top-level scalar tags with no way to hand-edit entries or address struct members (fixed — the map editor is now user-editable and a map entry's `tag` may be a dotted struct-member path, force-gated to scalar roots only). All three fixes are machine-proven end-to-end: the extended `tool/opcua_e2e.sh` asserts `NamespaceArray[1]` equals the project namespace and that a top-down Root→Objects browse reaches every tag, and the extended `tool/modbus_e2e.sh` asserts a forced coil reads back forced and a dotted struct-member holding-register entry decodes correctly.

**RTU-over-TCP framing (post-ship)** ✅ — a per-project selectable Modbus wire framing option (`ModbusProtocolConfig.framing`, additive, default `'tcp'`, a **Framing** dropdown on the Modbus TCP card) for masters that expect a serial-style RTU frame rather than an MBAP-framed TCP connection (e.g. behind a terminal server): no MBAP header, `unitId + PDU + CRC-16` (CRC low byte first), the request length derived purely from the function code since RTU carries no length field, a bad-CRC or unsupported-function-code frame dropped silently with the connection kept open (resync, mirroring a real outstation's silence rather than a torn-down link). Only one framing is served at a time per socket; the classic TCP/MBAP path is byte-identical and unaffected. **Machine-verified end-to-end** by a new `tool/modbus_rtu_e2e.sh` + `gateway/examples/modbus_rtu_probe.rs`: a real `tokio-modbus` crate **RTU** client, attached directly to a plain `TcpStream` via `tokio_modbus::client::rtu::attach_slave` (no MBAP anywhere in the client), completes `read_holding_registers` → `write_single_register` + independent read-back → `write_single_coil` + `read_coils` against the Dart fixture host running with `rtuOverTcp` framing selected — while `tool/modbus_e2e.sh`'s existing MBAP-framed run continues to pass unchanged. Serial (RS-485/RS-232) Modbus RTU remains out of scope — it needs a platform-specific serial plugin, which would break the pure-Dart design and is impossible on iOS. Documented in `docs/protocols/modbus.md`.

---

## Phase 6: MQTT Client & Sparkplug B Publisher ✅
- **Objective**: Telemetry publisher broadcasting tag updates to MQTT brokers.
- **Deliverables**:
  - JSON and Sparkplug B Protobuf payload support. ✅
  - Command topic (`/set`)/NCMD handling for remote writes (opt-in, default **off**). ✅
- **Status**: ✅ **SHIPPED — in-app pure-Dart MQTT 3.1.1 + Sparkplug B publisher CLIENT v1 (`MqttHost`, `mobile/lib/services/mqtt_host.dart`), machine-verified end-to-end against a real embedded `rumqttd` broker + a real `rumqttc` subscriber, in BOTH payload formats (`tool/mqtt_e2e.sh`; see `docs/protocols/MQTT.md`)**

**NDEATH on graceful disconnect (post-ship)** ✅ — a registered MQTT Will only fires on a *dead* connection; a clean MQTT `DISCONNECT` (the **Disconnect** button, or a normal app stop) tells the broker to *suppress* its own Will, so an intentional stop previously left a host/SCADA subscriber believing the node was still `ONLINE` forever. `MqttHost.disconnect()` now publishes an explicit death message first — an NDEATH (Sparkplug B) or retained `"OFFLINE"` (JSON) carrying the *current* session's bdSeq, best-effort, before the graceful `DISCONNECT` — while the registered Will remains exactly the safety net for an *ungraceful* drop it always was. **Machine-verified end-to-end** by a third phase added to `tool/mqtt_e2e.sh`/`gateway/examples/mqtt_probe.rs`: a real `rumqttc` subscriber watches a fresh Dart fixture connect, birth, then self-initiate (never killed by the probe) a clean stop mirroring `MqttHost.disconnect()` exactly, and asserts the NDEATH that arrives carries the *same* bdSeq the NBIRTH did — proof the app-level publish, not the broker's Will, is what put it there (the previous phase's forced-kill Will-triggered NDEATH is drained from the channel first so it can't be mistaken for this one). Documented in `docs/protocols/MQTT.md`.

---

## Phase 7: Touch HMI Controls & Mobile UI Polish
- **Objective**: Mobile gesture optimizations, responsive touch HMI controls, and mobile packaging.
- **Deliverables**:
  - **Responsive & adaptive layout** (width-based, not platform-based; shared `mobile/lib/ui/responsive.dart` with breakpoints 640/840, `context.isCompact`/`isExpanded`, viewport-clamped dialogs, 44px touch targets): the desktop 3-pane IDE is preserved at ≥840 px, and on a phone/narrow window the project tree becomes a `Drawer`, the Tag Inspector an end-drawer, editor palettes on-demand bottom sheets, graphical canvases pan/zoom viewers with tap-to-configure, and the Memory Manager a card list — verified by widget tests at fixed surface sizes plus a whole-app overflow smoke test (155 tests). ✅
  - **Editor polish** ✅ — the FBD workspace pans/zooms on every platform (was desktop-fixed) with an "Auto-arrange" action that lays blocks into tidy dependency-ordered columns (pure, tested layout); the HMI component dialog's dropdown overflow was fixed.
  - **Project-wide undo/redo** ✅ — a snapshot history (reusing the WS6 lossless serialization) captures on the debounced autosave tick (coalescing a drag/burst into one step), works across every editor with no per-editor plumbing, resets on project swap, and restores via `fromJson` with an editor-revision key that rebuilds the editor cleanly. Toolbar buttons + Ctrl/⌘+Z / +Y / +Shift+Z shortcuts; depth-capped, in-memory.
  - **Haptic feedback on HMI control press** ✅ — a `HapticFeedback.mediumImpact()` pulse when a `PushbuttonSwitch` is pressed or a `ToggleSwitch` is flipped in RUN mode (a no-op on desktop/web; fires on Android/iOS), gated by a global **SoftPLC Settings → Haptic feedback** toggle (default on, persisted via `SharedPreferences` `haptics_enabled`). Covered by widget tests asserting the platform haptic call fires only when enabled, plus the shell pref round-trip.
  - **Landscape-phone (short-viewport) chrome compaction** ✅ — a height-aware breakpoint layer (`mobile/lib/ui/responsive.dart`: `expandedMinHeight=600`, `shortHeight=500`, pure `isExpandedSize`/`isShortSize`, `context.isShort`) so the expanded 3-pane IDE now requires height ≥ 600 dp too (a short landscape phone stays single-pane instead of tripping a cramped 3-pane layout), and `isShort` drives compact chrome across **every** screen: slim 46 px app bars with shortened titles (`… (LD)`/`(FBD)`/`(SFC)`/`ST Code Editor`/`… — Memory`), inline mode switchers on the HMI and graphical editors, and a **collapsible Scan Speed bar** (an app-bar `Icons.speed` toggle + overflow-menu entry) so the toolbar can't eat half a landscape phone's height. Two landscape-only bugs fixed in passing: the Memory **Trends** tab overflowed a shallow body by ~64 px (now a single scrollable `ListView` with a size-aware preview height — 150 px when short, 220 px otherwise) and the SoftPLC Settings dialog's auto-focused Hz field popped a keyboard that hid the haptics toggle (autofocus removed). Verified live on-device (emulator) in **both** orientations across all four editors, the Memory tabs, Simulated I/O, the HMI builder, and their dialogs/pickers; a 900×410 short-landscape case guards the Trends fix in the overflow test suite.
  - Native mobile packaging (iOS/Android). ⏳ (see Phase 10)
- **Status**: 🔄 **ACTIVE — responsive/adaptive layout, editor polish (pannable FBD + auto-arrange), project-wide undo/redo, HMI haptic feedback, and landscape-phone chrome compaction shipped; native packaging remains**

---

## Phase 8: DNP3 Outstation Protocol Adapter ✅
- **Objective**: DNP3 outstation exposing Binary Inputs, Binary Outputs, Analog Inputs, and Analog Outputs to DNP3 masters.
- **Deliverables**:
  - Pure-Dart IEEE 1815 data-link (CRC-16/DNP, `0x0564` framing) + transport (segment reassembly) + application (object headers, static/control object codecs) layers. ✅
  - Outstation handler: Class 0 grouped-read (4 point types, force-aware), SELECT/OPERATE/DIRECT_OPERATE control (CROB + Analog Output Block), force-aware control rejection, g80v1 restart-clear. ✅
  - **Event classes + unsolicited reporting (input + output)** ✅ — per-point event Class 0/1/2/3 on all four point types, per-class bounded ring buffers with force-aware change detection, input **and** output change events (g2v2/g11v2 binary, g32v3/g32v7/g42v3/g42v7 analog; 48-bit absolute time), solicited Class 1/2/3 polls (CON + flush-on-CONFIRM), unsolicited responses (ENABLE/DISABLE fc20/21, CONFIRM + retry via `unsolConfirmTimeoutMs`/`unsolMaxRetries`/`eventBufferPerClass`), IIN class-available/overflow bits, and the DNP3-card event-Class dropdown (on input and output rows).
  - `dart:io` TCP host (`DnpHost`) + Outbound Protocols card (hosting controls, link-address config, point map editor). ✅
- **Status**: ✅ **SHIPPED — in-app pure-Dart DNP3 outstation v1 (`DnpHost`, `mobile/lib/services/dnp3_host.dart`), machine-verified end-to-end against a REAL third-party DNP3 master (Step Function I/O's `dnp3` crate, `tool/dnp3_e2e.sh`) — Class 0 poll, forced-value read, forced-tag control rejection, DIRECT_OPERATE, SELECT/OPERATE, plus solicited Class 1/2/3 event polls and outstation-initiated unsolicited events for BOTH input (g2/g32) and output (g11/g42) change events, all confirmed over the real wire (`DNP3 EVENTS PROBE PASS`; see `docs/protocols/DNP3.md`)**

---

## Phase 9: Advanced Process Simulation Engine ✅
- **Objective**: Built-in physical process models (PID loops, thermal dynamics, multi-tank flow systems).
- **Deliverables**:
  - **Analog-scaled rates** ✅ — an optional actuator tag (`sourcePath` + `refValue`) proportionally drives an `integrate`/`ramp` rate, so a PLC's analog output modulates the process (real closed-loop control); off/byte-identical when unset.
  - **First-order lag dynamics** ✅ — a `firstOrderLag` behaviour moves a value toward a target (fixed or tag-driven) with a time constant τ (realistic thermal/level/pressure response). Both are configurable in the Simulated I/O editor.
  - **Showcase** ✅ — the reactor temperature is a first-order thermal process (ambient pull + heat/cool) that the executed ST deadband controller regulates: reaches and holds setpoint, decays to ambient with control off (verified by a falsifiable closed-loop test).
  - **PID control function block** ✅ — a stateful FBD block (`SP,PV,KP,KI,KD → CV`) computing positional PID per scan (`error = SP−PV`, integral with conditional anti-windup, derivative, `CV` clamped 0–100), with per-block state in `FbdRuntime` (cleared on project switch, mirroring `TON`/`TOF`) and a palette entry. A "Tank Level PID Control" demo closes a real loop — the PID's `CV` drives an analog-scaled valve inflow against a constant outflow disturbance to hold level at setpoint (verified by a falsifiable closed-loop settling test; engine stays pure/never-throws even for pathological gain constants via the output clamp).
  - **IEC 61131-3 counter function blocks** ✅ — edge-triggered `CTU`/`CTD`/`CTUD` FBD blocks (`CU/R/PV→Q/CV`, `CD/LD/PV→Q/CV`, `CU/CD/R/LD/PV→QU/QD/CV`) with per-block state in `FbdRuntime` (cleared on project switch), reset/load priority, `CV` floored at 0, and palette entries. A "Batch Counter" demo counts pulsed part arrivals to a preset, raises `Batch_Done`, and self-resets via one-scan-delayed tag feedback (cycle-free) — verified by a falsifiable closed-loop counting test plus mutation-checked edge-trigger coverage.
  - **IEC 61131-3 edge detectors + pulse timer** ✅ — `R_TRIG`/`F_TRIG` (`CLK→Q` one-scan edge pulse) and the non-retriggerable `TP` pulse timer (`IN/PT→Q/ET`, fixed-width pulse using the scan clock), with per-block state in `FbdRuntime` (cleared on project switch) and palette entries. A "Pulse Output" demo gates a fixed 3000 ms `TP` pulse from a simulated button's rising edge (`R_TRIG`), so the output pulse width is independent of how long the button is held — verified by a falsifiable closed-loop test plus a mutation-locked non-retrigger/ET-hold test. This completes the standard IEC 61131-3 FBD function-block library (timers, PID, counters, edge/pulse).
  - **Transport dead-time + coupled tanks** ✅ — a `deadTime` Simulated-I/O behaviour outputs a source signal delayed by a dead time (`τ` seconds) via a bounded per-rule FIFO buffer (pass-through at `τ≤0`, clamped, force-aware, cleared on project switch), reusing existing rule fields (no serialization change) and configurable in the editor. A "Cascade Tanks with Transport Delay" demo couples two tanks through a 3 s transport line so the downstream tank visibly lags the upstream one — verified by pure engine step/ramp delay tests plus a direct `Transfer_Line == Tank_A[n scans ago]` assertion (mutation-checked to fail at `τ=0`).
  - **Measurement noise** ✅ — a `noise` Simulated-I/O behaviour reads a clean source tag and writes `measured = clean + bounded uniform noise (±amplitude)` to a *separate* tag, so it is non-accumulating (no random-walk drift). The noise is deterministic — a per-rule xorshift PRNG seeded from a stable FNV-1a hash of `rule.id` (no `Math.random`/clock) — so a project and its serialized round-trip produce the identical sequence and the 20-scan scan-equivalence guard stays green. Reuses existing rule fields (no serialization change), configurable in the editor. A "Noisy Level Measurement" demo shows a smooth true level, a jittery bounded measurement, and a first-order-lag-filtered reading — verified by bounded/varies/no-drift/determinism engine tests plus a filter-attenuation integration test.
  - **Nonlinear valve curves** ✅ — a selectable valve characteristic (`SimRule.valveCurve`, additive, default `linear`) transforms the analog-scaled-rate actuator fraction (`source/refValue`) before it scales an `integrate`/`ramp` rule's rate: `linear` (passthrough, default, byte-identical to the pre-feature behaviour, including out-of-range fractions), `equalPercentage` (`(50^f−1)/49`, convex — slow-then-fast) and `quickOpening` (`sqrt(f)`, concave — fast-then-flat), both clamped to `[0,1]`. Pure helper (`mobile/lib/models/valve_curve.dart`), a "Valve characteristic" dropdown in the Simulated I/O editor (shown only for an integrate/ramp rule with an actuator), and no change to any other persisted shape. See `docs/valve-curves.md`.
  - **Richer measurement noise + sensor drift** ✅ — a `noise` rule's per-scan noise term is now selectable between `uniform` (default, byte-identical to the original shipped behaviour) and `gaussian` (Box-Muller, standard deviation = amplitude), via the additive `SimRule.noiseDistribution` field. A separate, optional **bounded drift** term (`SimRule.driftAmplitude`, default `0` = off, and `SimRule.driftPeriodSec`, the drift's time-constant) models a slow sensor-bias wander — its own independent PRNG stream driving a convex EMA blend toward a random target, so it stays within `±driftAmplitude` by construction with no extra clamp. Both are configurable in the editor (Distribution dropdown; Drift amplitude field; Drift period (s) field shown only once drift amplitude is non-zero). See `docs/measurement-noise.md`.
  - **Pink (1/f) noise distribution** ✅ — a third `noiseDistribution` option, `pink`, models the slow-timescale, correlated wander of real instrument/process noise (energy falling off with frequency) instead of white/Gaussian's memoryless per-scan jitter. Implemented as a Paul Kellet one-pole filter cascade (`pinkStep`/`pinkNoise` in `noise_model.dart`), unconditionally stable (all pole coefficients `<1`), with per-rule filter state (`RuleRuntime.pinkState`, cleared on project switch like other per-rule state) seeded deterministically from the rule id, and normalised (`kPinkNormalise`) so `amplitude` means the output's standard deviation — matching Gaussian's sigma, so switching distributions keeps a comparable magnitude. Composes with drift exactly like the other distributions. Selectable in the editor's Distribution dropdown ("Pink (1/f)"); `uniform` with no drift stays byte-identical. See `docs/measurement-noise.md`.
  - **PID relay-feedback auto-tune** ✅ — a closed-loop relay-feedback (Åström–Hägglund) experiment (`mobile/lib/models/pid_autotune.dart`: `relayAutoTune`) drives a bang-bang relay around the operating point on a **deep copy** of the project's simulated process (the live loop and running scan are never touched), detects a sustained limit cycle (period + amplitude settled within 5% over 3 cycles), and estimates the ultimate gain `Ku = 4d/(π·a)` (`d` = relay half-amplitude, `a` = settled PV oscillation amplitude) and ultimate period `Pu`. `tuningRules` then computes six classic gain sets — Ziegler-Nichols, Tyreus-Luyben, and ZN no-overshoot, each in PID and PI form — from `Ku`/`Pu`. A new **PID Auto-Tune** panel (`mobile/lib/screens/pid_autotune_screen.dart`) picks any `PID` FBD block project-wide (`resolvePidLoop` traces the block's FBD wiring to auto-fill PV/CV/setpoint and locate the writable `CONST`/`TAG_INPUT` gain sources), runs the experiment, plots the recorded PV/CV trend, and **Apply** writes a chosen rule's gains directly onto the loop's `KP`/`KI`/`KD` sources. Both the experiment and the tuning-rule math are pure and deterministic (no clock, no randomness) — verified against the "Tank Level PID Control" demo. Documented in [`docs/pid-autotune.md`](docs/pid-autotune.md).
  - **MIMO coupled plant + interaction analysis** ✅ — a genuine 2×2 multivariable process, the **"MIMO — Two Thermal Zones"** default project: two heaters/temperatures (`Heater_A`/`Temp_A`, `Heater_B`/`Temp_B`) built from stacked Simulated I/O rules — each zone's own `integrate` heater input, a short-time-constant `firstOrderLag` conducting heat toward the *other* zone (the coupling path), and a slower `firstOrderLag` heat loss toward ambient — so stepping `SP_A` visibly moves `Temp_B`. A new **Interaction Analysis** panel (`mobile/lib/screens/interaction_analysis_screen.dart`, backed by pure/deterministic `mobile/lib/models/interaction_analysis.dart`) runs automated open-loop step tests on a **deep copy** of the plant (`identifyGainMatrix`, never touching the live loop) to identify the 2×2 steady-state gain matrix `K`, then computes the Relative Gain Array (`computeRga`: `λ11 = K11·K22 / (K11·K22 − K12·K21)`) to quantify interaction and recommend a loop pairing (`λ11` near 1 = low interaction/diagonal pairing; far from 1 = strong interaction, decoupling recommended; a near-singular `K` is reported as ill-conditioned). The control program's static decoupler (`Heater_A = LIMIT(0, u_A − d12·u_B, 100)`, `Heater_B` symmetric) ships with `d12`/`d21` `CONST` blocks defaulted to `0` (fully coupled); setting them to the RGA-suggested `K12/K11`/`K21/K22` gains cancels most of the cross-effect — the demo's identified gains (`K12/K11 ≈ K21/K22 ≈ 0.833`) cut the peak cross-loop disturbance from an `SP_A` step by roughly **51%** (verified in `test/mimo_project_test.dart`: coupled ≈13.6 °C peak `Temp_B` deviation vs decoupled ≈6.7 °C). Documented in [`docs/mimo-coupled-plant.md`](docs/mimo-coupled-plant.md).
- **Status**: ✅ **COMPLETED — analog rates, first-order lag, PID, the full IEC FBD function-block library (timers/counters/edge/pulse), transport dead-time + coupled tanks, deterministic measurement noise (uniform + Gaussian + pink, with optional bounded sensor drift), nonlinear valve curves, the relay-feedback PID auto-tuner, and the MIMO coupled-plant + interaction-analysis (gain matrix/RGA/static decoupler) all shipped with showcase demos — Phase 9 complete**

---

## Phase 10: Packaging, Multi-Platform Clean Builds & Documentation
- **Objective**: Native installers for Windows (`.msi`), macOS (`.dmg`), Linux (`.AppImage`), Android (`.apk`/`.aab`), and iOS (`.ipa`).
- **Deliverables**:
  - **Native app readiness** ✅ — the single Flutter codebase now has all five platform targets scaffolded (android/ios/windows/macos/linux + web), consistent app identity (**Soft PLC Simulator**, `com.jarrodfranz.softplcsimulator`, `0.1.0+1`), a brand-free ladder-logic launcher icon + dark splash generated for every platform, and a [SHIPPING.md](../../SHIPPING.md) guide covering the build commands and the user-owned steps (developer accounts, signing keys, store uploads) plus the local toolchain gaps. Web builds today.
  - Signed store binaries + installers, and completing the local/CI build toolchains. ⏳ (user-owned: accounts, certificates, uploads; iOS/macOS need a Mac).
- **Status**: 🔄 **ACTIVE — native scaffolding/identity/icons/splash + shipping docs done; signed store builds remain (user-owned toolchain + accounts)**

---

## Phase 11: Task-Type Scheduler, Per-Task Watchdog & System Diagnostics Tag ✅
- **Objective**: Move every default project off an implicit single Continuous scan onto a real IEC 61131-3 multi-task model — Startup/Event/Periodic/Continuous tasks with a fixed priority order, a per-task watchdog with fault + recovery, free-run scanning, and a reserved `System` diagnostics tag — with full task-management UI.
- **Deliverables**:
  - **Pure priority task scheduler** ✅ (`mobile/lib/models/task_scheduler.dart`) — decides which tasks are due each scan tick in strict `Startup > Event > Periodic > Continuous` priority order: Startup fires once per run session, Event fires on the rising edge of a bound `BOOL` trigger tag (`PlcTask.triggerTag`), Periodic accumulates elapsed time against `periodMs` with clamped carry-forward (no unbounded catch-up burst), and Continuous is skipped on any tick where a higher-priority task is due. A program claimed by a higher-priority due task in the same tick is de-duplicated out of any lower-priority task's run.
  - **Per-task watchdog** ✅ (`mobile/lib/screens/scan_tick.dart`) — `PlcTask.watchdogMs` (0 = disabled) measures actual wall-clock execution time per due task; exceeding it latches a fault (task name + fault code), halts the scan loop, and surfaces a fault banner. Two independent recovery paths: the in-app **Clear Fault** button, and an external `System.AlarmReset` write consumed by a background supervisor poll that keeps working even while the scan loop itself is halted — so a faulted controller can be recovered purely from SCADA/HMI.
  - **Free-run scan mode** ✅ — a toggle alongside the existing fixed `scanSpeedMs` timer: free-run re-arms a zero-delay timer after each tick (yielding to the UI event loop between scans) and feeds the scheduler *measured* elapsed time as `dtMs` instead of a fixed constant, so Periodic accumulation and time-based simulation see real elapsed time.
  - **`System` diagnostics UDT** ✅ (`mobile/lib/models/system_tags.dart`) — a reserved `System` tag / built-in `SYSTEM` composite (alongside `TIMER`/`COUNTER`), auto-injected and back-filled on project load: `Fault`/`FaultTask`/`FaultCode`/`Running`/`FirstScan`/`ScanCount`/`ScanTimeMs`/`MaxScanTimeMs`/`MinScanTimeMs`/`FreeRun`/`UptimeMs` status fields, wall-clock `Year`/`Month`/`Day`/`Hour`/`Minute`/`Second`/`DateTime`, and the one writable control field `AlarmReset` for external fault-reset. Reserved-name protection prevents the tag from being renamed/retyped/deleted as an ordinary user tag.
  - **Task-management UI** ✅ — task tree with add/edit/delete (no-orphan guard: a task cannot be deleted while it still owns programs), and the Add-Program dialog's "＋ New task…" flow to create Startup/Event/Periodic/Continuous tasks (with period/trigger-tag/watchdog fields as applicable) inline while assigning a new program.
  - Full green gate (`flutter test`, `flutter analyze`, `flutter build web --release`) and a dedicated round-trip guard proving `PlcTask.triggerTag`/`watchdogMs` and the `System` tag's full field set survive `toJson`/`fromJson` — documented in [`docs/task-scheduling.md`](docs/task-scheduling.md). ✅
- **Status**: ✅ **COMPLETED — priority task scheduler, per-task watchdog with dual recovery paths, free-run scanning, the `System` diagnostics UDT, and task-management UI all shipped and documented**

---

## Phase 12: Bulk Simulated Test-Tag Generation ✅
- **Objective**: Stand up N phase-staggered simulated signals in one action (for load-testing a protocol client or quickly populating a demo/test project) instead of hand-adding tags one at a time, without opening a new pathway for a program or an outbound protocol master/client to actually control one.
- **Deliverables**:
  - **Folders** ✅ — a flat `PlcTag.folder` display-grouping label (no nesting, no separate `Folder` model); the Memory Manager's tag list and every protocol map editor group rows by folder (root tags first, unlabeled; every other folder a collapsible, alphabetically-ordered, count-labeled section) via the shared `groupEntriesByFolder` helper.
  - **`SignalGen` + the seven-waveform signal engine** ✅ (`mobile/lib/models/signal_gen.dart`, `mobile/lib/models/signal_engine.dart`) — `ramp`/`sine`/`square`/`triangle`/`counter`/`toggle`/`random` waveforms driven every scan tick by `applySignalGens` (a `PlcProject.signalGens` list, additive alongside the pre-existing `simRules`), with even phase-staggering (`phase = i/count`) and a deterministic (FNV-1a-seeded xorshift32, not `Math.random`) `random` draw so serialization round-trips and reruns stay byte-reproducible.
  - **Hard read-only enforcement, both sides** ✅ — a generated tag's path is refused by every language executor's write path (`generatedPaths(gens)` passed as the shared `readOnly` set into `executeLdPrograms`/`executeFbdPrograms`/`executeSfcPrograms`/`executeStPrograms`, a silent no-op exactly like an existing `SimulatedOutput` write-from-logic refusal) **and** every generated tag is always mapped read-only over every outbound protocol (`ioType: 'SimulatedOutput'` + `ReadOnly`/non-writable map entries on OPC UA/Modbus/DNP3/MQTT alike), so a generated signal can only ever be driven by its own generator.
  - **`buildTestSet` + four `appendTo*Map` protocol auto-mappers** ✅ (`mobile/lib/models/test_tag_set.dart`) — builds `count` zero-padded, phase-staggered tags + generators from one `TestSetSpec`, then appends them onto any already-configured OPC UA/Modbus/DNP3/MQTT map at the next-free address/index/slot (conservative worst-case-width bookkeeping for Modbus's ambiguous register-table entries), never duplicating or overlapping an existing entry.
  - **Generate Test Set dialog + folder delete-set** ✅ (`mobile/lib/screens/memory_manager_screen.dart`) — one dialog collects folder/base-name/count/type/range/period plus a per-configured-protocol checkbox and validates (non-empty folder, `min < max`, no tag-name collision) before generating; a folder's delete affordance removes the folder's tags, their generators, and their entries across all four protocol maps in one confirmed action.
  - Full green gate (`flutter test`, `flutter analyze`, `flutter build web --release`), a dedicated round-trip guard proving `PlcTag.folder` and `PlcProject.signalGens` survive `toJson`/`fromJson`, and a **machine-verified end-to-end** extension of `tool/modbus_e2e.sh`/`gateway/examples/modbus_probe.rs`: the same real Rust `tokio-modbus` client reads two phase-staggered generated `ramp` tags (`buildTestSet` + `appendToModbusMap`, driven by `applySignalGens` on a real per-tick clock in the Dart fixture host) and asserts they read distinct live values at the same instant *and* that one of them visibly moves between two reads spaced apart in wall-clock time (`MODBUS PROBE PASS`) — documented in [`docs/simulated-test-tags.md`](docs/simulated-test-tags.md).
- **Status**: ✅ **COMPLETED — folders, the seven-waveform signal engine with phase-staggering and deterministic randomness, hard read-only enforcement (program writes + protocol-wire access), per-protocol auto-map with next-free allocation, the Generate Test Set dialog + folder delete-set, and the round-trip/E2E machine-proofs all shipped and documented**

**Folder browsing over the wire (post-ship)** ✅ — the flat `PlcTag.folder` grouping above now also shapes how two outbound protocols organize the same tags for a live client, not just the Memory Manager/map-editor UI: OPC UA synthesizes a **FolderType** Object node per distinct folder (browse name = folder name, reserved `ns=1;s=__folder__/<folder>` node id that can never collide with a real tag's `ns=1;s=<tagName>`) organized directly under the standard Objects folder, holding that folder's tags as Organizes/Variable children — root tags (`folder: ''`) stay directly under Objects exactly as before, so a project with no folders browses byte-identically to pre-feature behavior (`mobile/lib/protocols/opcua/opcua_address_space.dart` + `opcua_services.dart`). MQTT/Sparkplug B prefixes a foldered tag's published metric name to `'<folder>/<name>'` (root tags unaffected; the map entry's underlying `tag` field stays the bare name) so a Sparkplug-aware subscriber groups the same folders in its own tag browser (`MqttMap.autoGenerate`/`appendToMqttMap`). No persisted model changed — `PlcTag.folder` already round-tripped; the MQTT metric is regenerated, not a new persisted field. **Machine-verified end-to-end** by the extended `tool/opcua_e2e.sh`: the real Rust `opcua` crate client browses Objects, finds the folder as a `NodeClass::Object`, browses into it, and reads a tag inside it. Documented in `docs/simulated-test-tags.md` and `docs/protocols/opcua.md`.

**Composite/`System` tag exposure + MQTT publish tuning (post-ship)** ✅ — a composite tag (a struct, an array, or the reserved `System` diagnostics UDT — Phase 11) was previously either skipped entirely by a protocol's auto-map or exposed only as one opaque, unreadable entry; a shared `scalarLeaves` resolver (`mobile/lib/models/tag_resolver.dart`) now walks every tag down to its scalar leaves, and all four `autoGenerate`s (OPC UA/Modbus/DNP3/MQTT) iterate those leaves instead of top-level tags, adding one map entry per leaf at a dotted/indexed path (`System.Fault`, `System.ScanTimeMs`, `Recipe_Steps[0]`, `Motor.Speed`, ...) — a scalar-only project's map is byte-identical to before. OPC UA's address space resolves these dotted paths live (`opcua_address_space.dart`) through the same force-aware `readPath`/`writePath` every adapter shares, while still reconstructing each root tag's folder-qualified node-id prefix so no previously-shipped scalar node id changes. `STRING` leaves (e.g. `System.DateTime`) are now exposed on OPC UA **and** MQTT; Modbus/DNP3 continue to skip `STRING`/`TIMER`/`COUNTER` leaves (no wire representation defined there). `System.*` is always read-only on the wire on every protocol (the reserved tag's explicit `access: 'ReadOnly'`, independent of `ioType`, is checked by every map's read-only rule). Separately, MQTT gained a configurable **publish interval** (`publishIntervalMs`, default 250 ms, floor 20 ms — re-armed on reconnect) and an optional **analog deadband** (`deadband`, default 0 = off, non-drifting baseline, numeric tags only) to reduce broker/network load on large maps, plus a shared `NotifyThrottle` (`mobile/lib/services/notify_throttle.dart`) coalescing the high-frequency per-tick UI notification to a trailing ~250 ms window while status changes/rebirths still notify immediately. **Machine-verified end-to-end** by the extended `tool/opcua_e2e.sh`: the real Rust `opcua` crate client reads the auto-generated `System.Fault` (Boolean) and `System.ScanTimeMs` (Double) nodes live off the running fixture host. Documented in `docs/protocols/opcua.md`, `docs/protocols/MQTT.md`, `docs/protocols/modbus.md`, and `docs/protocols/DNP3.md`.

**UI repaint decoupling — gateway virtualization, `LiveTick`, configurable refresh rate, Tag-Inspector folders (post-ship)** ✅ — with a large tag count (e.g. a 100-tag generated load-test set), several outbound protocols enabled, and the gateway panel open and transmitting, the workspace previously stuttered because every scan tick called `setState` on the entire workspace shell. Four changes fixed this: (1) the Outbound Protocols panel now scopes each host's `ListenableBuilder` to just its status/connection subtree instead of wrapping the whole map, and its per-protocol tag/point map is now a virtualized `ListView.builder` so only on-screen rows are ever built (`mobile/lib/screens/gateway_screen.dart`). (2) A new `LiveTick`/`LiveTickScope` (`mobile/lib/widgets/live_tick.dart`) replaces the per-scan whole-shell `setState`: the scan loop writes the tag model directly and requests a throttled pulse (reusing the existing `NotifyThrottle`) instead of rebuilding the tree, and every live-value surface — Tag Inspector cells, Memory Manager's Live Value column, HMI dashboard components, editor run overlays, the toolbar Scan Count, the fault banner — wraps just its value leaf in a `ListenableBuilder` on that pulse, so only on-screen live-value widgets repaint. A targeted `setState` remains only for genuine structural transitions (a fault tripping/clearing). (3) The repaint rate is now a user setting — **SoftPLC Settings → UI refresh rate (Hz)**, default 10 Hz, range 1–30 Hz, persisted globally via `SharedPreferences` (`ui_refresh_hz`) and applied immediately (`mobile/lib/screens/workspace_shell.dart`, `mobile/lib/screens/softplc_settings_dialog.dart`). (4) The Tag Inspector groups tags by folder with collapsible, count-labeled section headers (reusing the same `groupEntriesByFolder` helper as the protocol map editors), so a heavily-foldered project isn't one long undifferentiated list. Covered by dedicated widget tests (`gateway_panel_perf_test.dart`, `live_tick_test.dart`, `tag_inspector_livetick_test.dart`, `memory_manager_livetick_test.dart`, `livetick_surfaces_test.dart`, `scan_no_shell_rebuild_test.dart`, `refresh_rate_pref_test.dart`, `tag_inspector_folders_test.dart`, `fault_banner_test.dart`) plus the full existing suite, proving every live surface still updates with the shell-level `setState` removed. Documented in [`docs/ui-performance.md`](docs/ui-performance.md).

---

## Phase 13: Tag Historian & Trend Charts ✅
- **Objective**: Record selected tags into a live, in-app time-series buffer and plot them — a **Trends** section under Memory to manage what's recorded, and a trend chart both there and as an HMI dashboard component — so a user can watch and inspect signal history without an external historian.
- **Deliverables**:
  - **Memory-only, tick-driven historian** ✅ (`mobile/lib/services/tag_historian.dart`) — a pure-Dart `TagHistorian` keeping one ring buffer of `(timestamp, value)` samples per configured pen, sampled off the existing scan tick (deterministic, no `Timer`), with per-pen retention by **either** a max point count **or** a max time window. Buffers live only in RAM: captured samples are **never** persisted and reset to empty on project switch, and sampling advances only while the PLC is running.
  - **Pens (persisted config, transient data)** ✅ — a `TrendPen` (tag path + color + sample interval + retention) list on `PlcProject.trends`, plus additive `HmiComponent.trendPens`/`windowMs` for the HMI component — all additive JSON (`trends`/`trend_pens`/`window_ms`); older projects load clean and the pen *config* round-trips while the sample *data* is never serialized.
  - **Trends section under Memory** ✅ — a third tab (alongside Global Tags and Struct Definitions) to add/edit/remove pens (type-ahead tag picker, color, interval, points-or-time retention) with a **live preview chart** to watch and tune.
  - **Hand-painted multi-pen trend chart** ✅ (`mobile/lib/widgets/trend_chart.dart`, a `CustomPainter` — no chart library) — analog pens auto-scaled on a shared value axis drawn as connected lines; BOOL pens drawn as stacked 0/1 digital step lanes; a newest-on-the-right auto-scrolling time axis. Repaints via `LiveTick` (Phase 12's decoupled-repaint architecture), never a whole-shell `setState`.
  - **HMI `TrendChartDisplay` component** ✅ — the first multi-pen, over-time HMI component: a palette entry + config dialog to select which project pens to plot, an optional per-pen color override, and a component window; both the Trends preview and the HMI component render through one shared `TrendChartView`.
  - **Draggable trace cursor** ✅ — a SCADA-style scrubber on every trend chart: tap to place / horizontal-drag to move (touch on mobile + mouse on desktop, without blocking vertical page scroll); a readout shows the cursor time **both** relative (`-1m 12s`) and wall-clock (`HH:mm:ss`) plus each pen's nearest-sample value (`ON`/`OFF` for BOOL, `—` when absent); the cursor anchors to a timestamp so it sticks to the data as the chart scrolls and **auto-hides** once that moment scrolls off the left edge; a ✕ clears it. Transient UI state (nothing persisted); shared `TrendChartGeometry` keeps the cursor hit-test and the painted x-axis from drifting.
  - Full green gate (`flutter test`, `flutter analyze`, `flutter build web --release`), round-trip guards proving the pen config survives `toJson`/`fromJson` while samples are never serialized, and dedicated painter/interaction widget tests (analog auto-scale, digital lanes, cursor place/drag/clear, auto-hide, overflow at 320/360/1400). Documented in [`docs/trends.md`](docs/trends.md).
- **Status**: ✅ **COMPLETED — memory-only tick-driven historian, the Trends section with live preview, the multi-pen trend chart (analog auto-scale + BOOL digital lanes), the HMI `TrendChartDisplay` component, and the draggable trace cursor all shipped and documented**

---

## Phase 14: Protocol Expansion Program — EtherNet/IP + CIP (workstream 2 of 6) ✅
- **Objective**: Extend the shipped four-protocol suite (OPC UA, Modbus TCP, MQTT + Sparkplug B, DNP3) into a **protocol-expansion program** covering the remaining protocols an integrator is most likely to need — Modbus RTU-over-TCP (WS1), **EtherNet/IP + CIP** (WS2), S7comm (WS3), FINS (WS4), SLMP (WS5), and BACnet/IP (WS6) — each hosted in-app in pure Dart per ADR-010, each proven against a real third-party client. This phase records the program and delivers its second workstream. (The program's six workstreams and their order are defined in [`docs/superpowers/specs/2026-07-16-protocol-expansion-program-roadmap.md`](docs/superpowers/specs/2026-07-16-protocol-expansion-program-roadmap.md).)
- **Program shape**: every protocol follows the same six-task shape — a pure-Dart wire codec built bottom-up in independently-testable layers, a `dart:io` socket host as the ONLY file allowed to touch sockets, a tag-exposure map model mirroring the existing `OpcuaMap`/`ModbusMap` precedent (including the force-aware, VISIBLE write refusal), additive persistence (no protocol key in a project's JSON ⇒ disabled, default port), and a real-third-party-client E2E that is treated as **the authority on wire details** over any unit test.
- **Delivered — EtherNet/IP + CIP explicit messaging (v1)** ✅ (TCP 44818, default off):
  - **Encapsulation layer** ✅ (`mobile/lib/protocols/enip/enip_encap.dart`) — the 24-byte little-endian encapsulation header and the Common Packet Format (CPF) item list, plus `NOP`/`RegisterSession`/`UnRegisterSession`/`SendRRData`/`SendUnitData`.
  - **CIP messaging layer** ✅ (`cip.dart`) — the CIP request/response envelope, EPATH parse/build (the ANSI Extended Symbol segment `0x91` that carries a tag's symbolic name, plus logical Class/Instance/Attribute segments in both 8- and 16-bit forms), and the elementary data-type codec (`BOOL`/`INT`/`DINT`/`LINT`/`REAL`).
  - **Connection manager** ✅ (`cip_connection.dart`) — Forward Open (0x54) and Forward Close (0x4E), with **monotonic** (never random, never clock-derived) connection-id allocation so runs are byte-reproducible. The Large Forward Open (0x5B) is deliberately not implemented; a client that tries it first receives 0x08 and falls back.
  - **Tag services + `CipMap`** ✅ (`cip_tags.dart`, `mobile/lib/models/cip_map.dart`) — Read Tag (0x4C), Write Tag (0x4D), and the Multiple Service Packet (0x0A) batch, addressed **symbolically** by name and resolved through the same shared `models/tag_resolver.dart` every other adapter uses. Composite/array tags are pre-expanded to one map entry per scalar leaf. A write to a `ReadOnly`-mapped entry **or to a FORCED tag** is refused with a visible 0x0F Privilege Violation and leaves the tag unchanged — never a silent drop.
  - **Socket host + UI** ✅ (`mobile/lib/services/enip_host.dart`) — frame reassembly across split and coalesced TCP chunks, per-socket session handles and connection managers, non-throwing codecs throughout (a hostile frame drops only its own connection), and an Outbound Protocols entry with a map editor. Persistence is additive: a project with no `ethernet_ip` key loads disabled on 44818.
  - **Real-client E2E + the Python probe lane** ✅ — `tool/enip_e2e.sh` runs the third-party Python **`pycomm3`** client (pinned `pycomm3==1.2.16`) against a pure-Dart fixture host and asserts RegisterSession → Large-Forward-Open rejection and fallback → Forward Open → Read Tag (every supported type) → Write Tag → **independent read-back of the exact written value** → unconnected (UCMM) read → ReadOnly/forced write refusals → Forward Close. **The E2E found a real defect no unit test could**: Forward Open returned the two connection ids in the wrong direction (the target must ALLOCATE the O→T id a client addresses connected messages to, not echo the client's zero placeholder), so every `SendUnitData` from a real client was unroutable — fixed in `cip_connection.dart`, with the consumer-allocates rule now documented at the source. This also establishes the **shared Python lane** (`tool/py/`, venv-isolated, exact-pinned requirements, git-ignored virtualenv) that the S7comm, FINS, SLMP, and BACnet workstreams reuse.
  - Full green gate (`flutter analyze` clean, `flutter test`, `flutter build web --release`) plus no regression in `tool/modbus_e2e.sh` / `tool/opcua_e2e.sh`. Documented in [`docs/protocols/ethernet-ip.md`](docs/protocols/ethernet-ip.md).
- **Deferred to EtherNet/IP v2 (documented, deliberate)**: Symbol/Template Object browse (controller tag-list upload), `STRING` as a structured type (it needs the Template Object to describe its layout), the Identity Object / `ListIdentity`, the Large Forward Open, and implicit (Class 1 I/O) UDP messaging.
- **Remaining workstreams**: SLMP, BACnet — each reusing the six-task shape and the Python probe lane above (S7comm shipped as WS3 and FINS as WS4, below). ⏳
- **Status**: ✅ **COMPLETE — EtherNet/IP + CIP explicit messaging (v1) shipped, real-client E2E-proven, and documented**

---

## Phase 15: Protocol Expansion Program — S7comm (workstream 3 of 6) ✅
- **Objective**: Deliver the program's S7comm workstream — host S7comm on TCP 102 in-process so a SCADA S7 driver can read and write the app's tags by **memory area + byte offset**, following the same six-task shape and reusing the Python probe lane **EtherNet/IP (WS2)** established. (WS1, Modbus RTU-over-TCP, used the Rust client lane.)
- **Delivered — S7comm device side (v1)** ✅ (TCP 102, default off):
  - **TPKT + COTP framing** ✅ (`mobile/lib/protocols/s7/tpkt_cotp.dart`) — RFC 1006 TPKT, whose big-endian `length` counts the **whole** packet including its own 4-byte header (the exact inverse of EtherNet/IP's encapsulation length, which excluded it), plus COTP Connection Request → Connection Confirm with TSAP and TPDU-size parameters, and Data TPDUs. Rack/slot are accepted permissively — this is a simulator, and rejecting a mismatch gives a confusing failure with no diagnostic value.
  - **S7 PDU + Setup Communication** ✅ (`s7_pdu.dart`) — the header, **12 bytes on `Ack_Data` and 10 on every other ROSCTR**, PDU-length negotiation that only ever moves DOWN from the client's proposal (clamped to a documented 240-byte floor), and the Read/Write Var item specifications and data items.
  - **Area byte image + `S7Map`** ✅ (`s7_area_image.dart`, `mobile/lib/models/s7_map.dart`) — S7comm addresses a **byte range**, not a name or a register, so tags are packed into an area image that is sliced on read and decoded back onto tags on write. Areas `DB`/`M`/`I`/`Q`; `BOOL` bit-packed; `FLOAT64` narrowed to a 4-byte `REAL`. Unmapped bytes read zero and discard writes; a partially covered tag is left unchanged **and reported**; a write to a `ReadOnly` entry or a **FORCED** root tag is refused with the tag untouched.
  - **Socket host, shared dispatch, and UI** ✅ (`mobile/lib/services/s7_host.dart`, `s7_services.dart`) — TPKT reassembly across split and coalesced chunks, per-socket COTP/S7 state, non-throwing codecs (a hostile frame drops only its own connection), and an Outbound Protocols card with an area-map editor. Read/Write Var response bytes are produced by **one shared pure function both the host and the E2E fixture call**, so the third-party client's verdict applies to the shipped host by construction rather than by diff. Persistence is additive: a project with no `s7comm` key loads disabled on 102.
  - **Real-client E2E** ✅ — `tool/s7_e2e.sh` runs the third-party Python **`python-snap7`** client (pinned `python-snap7==3.1.0`) against a pure-Dart fixture host through connect → negotiate → read → write → **independent read-back of the exact written value**, across two memory areas, a single-bit round trip, an odd-length read, gap semantics, and both write-refusal paths. The client ran **at task 3 of 5**, before any read/write logic existed, so the handshake was proven on the wire before anything was layered on it. **The E2E settled two wire details no unit test structurally could**: a BIT data item must declare `bytes * 8`, not a bit count (a round-trip cancels the difference, since the parser recovers `(declared + 7) ~/ 8` either way — the client recovered *zero* bytes from a declared `1`), and the trailing pad byte on a response's final data item is accepted rather than rejected.
  - **Port 102 is privileged** on Linux/macOS: a denied bind is surfaced as its own labelled failure block rather than a card that merely failed to turn green, and the port is user-editable as the unprivileged workaround.
  - Full green gate (`flutter analyze` clean, `flutter test`, `flutter build web --release`) plus no regression in `tool/enip_e2e.sh` / `tool/modbus_e2e.sh` / `tool/opcua_e2e.sh`. Documented in [`docs/protocols/s7comm.md`](docs/protocols/s7comm.md).
- **Deferred to S7comm v2 (documented, deliberate)**: `STRING` (an S7 `STRING` is a struct with max-length/actual-length header bytes, not a byte run), the timer and counter areas (S5TIME/BCD encodings with no equivalent in this app's tag model), and optimized-block access (a different addressing scheme — symbolic, with no stable byte offsets — rather than a tweak to this one).
- **Status**: ✅ **COMPLETE — S7comm (v1) shipped, real-client E2E-proven, and documented**

## Phase 16: Protocol-Hardening Pass — write-gating + response-size budgets ✅
- **Objective**: Fix two classes of defect that **two audits** surfaced across the already-shipped protocol hosts, without changing any behaviour except at the specific limit each fix targets.
  - **Audit A — external-write gating.** The six auto-generated read-only rules and seven write-time gates each trusted a **mutable map entry** rather than re-checking the underlying tag, so a hand-edited map entry could re-target the reserved `System` tag or a tag whose own `access` had since become `ReadOnly` and still be written. A shared pure write-gate predicate (`isExternallyWritable`) now re-checks the tag itself at every write path; `System` is a hard, non-overridable block while a `SimulatedOutput` override deliberately survives. Modbus additionally now **visibly refuses** a forced write (exception PDU) instead of silently echoing success.
  - **Audit B — response-size overruns.** Three hosts could emit a reply larger than the size they had negotiated with the client, because the negotiated limit was parsed-and-discarded (or never parsed). Each fix mirrors the S7 Read Var budget already in the tree (charge each item's full on-wire cost at admission; reserve the mandatory minimum for every still-to-come item; answer an over-budget item with a per-item error rather than an oversized frame): **DNP3** real application-layer multi-fragment on the solicited read path, **OPC UA** fail-loud `Bad_ResponseTooLarge`, and **EtherNet/IP** the Multiple Service Packet reply bounded by the Forward Open **T→O connection size** (the size word was previously skipped entirely). EtherNet/IP also tightened the MSP reply-cursor `u16` guard from `> 0xFFFF` to `> 0xFFFF - 6` to account for the 6-byte outer framing.
- **Every bound ships a boundary test at its measured tipping point** — the audits' core finding was that all four negotiated limits had **zero** coverage. The four third-party E2Es (`python-snap7`, `pycomm3`, `tokio-modbus`, the Rust `opcua` client) remain the wire authority and all pass.
- **Deliberately deferred (named so their absence reads as a decision, not an oversight)**:
  - **OPC UA send-path Chunk/`F`-`C` chunking** — the send path still emits single-chunk `F` messages; splitting a large response across `C` continuation chunks is a larger change and is not required for the fail-loud bound, which refuses an over-large response cleanly rather than truncating it.
  - **OPC UA inbound 16 MB-buffer memory bound** — the inbound reassembly buffer is not yet capped against a hostile `maxMessageSize`; tracked as a follow-up task.
  - **DNP3 multi-fragment on the unsolicited path** — only the **solicited** read path fragments in this pass; the unsolicited path stays single-fragment, tracked as a follow-up task.
- **Status**: ✅ **COMPLETE — both audits closed, all four real-client E2Es green, documented in each protocol's page**

## Phase 17: Protocol Expansion Program — Omron FINS (workstream 4 of 6) ✅
- **Objective**: Deliver the program's FINS workstream — host Omron FINS over **UDP 9600** in-process so a SCADA FINS driver can read and write the app's tags by **memory area + word offset**, following the same task shape and reusing the Python probe lane. FINS is the suite's **first datagram (UDP) protocol** — every prior host is TCP.
- **Delivered — FINS device side (v1)** ✅ (UDP 9600, default off):
  - **FINS frame + Memory Area codec** ✅ (`mobile/lib/protocols/fins/fins_frame.dart`, `fins_memory.dart`) — the 10-byte header (whose response swaps `DNA/DA1/DA2` ↔ `SNA/SA1/SA2` and echoes the `SID`), and the Memory Area Read (`0x0101`) / Write (`0x0102`) item specs. Node addressing is accepted permissively; the codecs never throw on hostile input.
  - **Area word image + `FinsMap`** ✅ (`fins_area_image.dart`, `mobile/lib/models/fins_map.dart`) — FINS addresses a **word** (2 bytes), so tags are packed into a word image sliced on read and decoded back onto tags on write. Areas `DM`/`CIO`/`WR`/`HR`; `BOOL` bit-packed (0..15); `FLOAT64` narrowed to a 2-word `REAL`. Unmapped words read zero and discard writes; a partially covered tag is left unchanged **and reported**; a write to a `ReadOnly` entry or a **FORCED** / reserved-`System` root tag is refused (shared write-gate `isExternallyWritable`, no duplication).
  - **UDP socket host, shared dispatch, config, and UI** ✅ (`mobile/lib/services/fins_host.dart`, `fins_dispatch.dart`, `protocol_settings.dart`, `gateway_screen.dart`) — a single `RawDatagramSocket` (no reassembly, no per-connection state; one datagram = one frame; a reply correlated by source address + echoed `SID`), non-throwing per-datagram handling (one bad packet never wedges the bind), a persisted `FinsProtocolConfig` (JSON key `fins`, additive: a project with no `fins` key loads disabled on 9600), and an Outbound Protocols card with an area-map editor. Read/Write response bytes are produced by **one shared pure function both the host and the E2E fixture call**, so the third-party client's verdict applies to the shipped host by construction.
  - **Real-client E2E** ✅ — `tool/fins_e2e.sh` runs the third-party Python **`fins`** client (pinned `fins==1.0.5`) against a pure-Dart fixture host through connect → read → **write → independent read-back of the exact written value**, across DM and CIO areas, a BOOL bit, a 32-bit `DINT`, a `REAL`, and a `ReadOnly` write refusal. **The E2E settled the 32-bit two-word order a round-trip structurally could not**: the fixture **seeds** a known `DINT` into a tag independently of the client, and the `fins` client's own two-word decode read it back — proving the client is **LOW-WORD-FIRST** (it word-reverses a multi-word value), which **overturned the provisional high-word-first choice**. The order was flipped in the two `fins_area_image.dart` helpers and the literal-byte tests, and re-proven.
  - **Port 9600 is unprivileged** (above 1023), so unlike S7comm's port 102 there is **no** privileged-port caveat on any native platform. The card carries the UDP/web note instead: a browser cannot bind an inbound UDP port, so hosting is native-only.
  - Full green gate (`flutter analyze` clean, `flutter test` 2020 passing, `flutter build web --release`) plus no regression in `tool/s7_e2e.sh` / `tool/enip_e2e.sh` / `tool/modbus_e2e.sh` / `tool/opcua_e2e.sh`. Documented in [`docs/protocols/fins.md`](docs/protocols/fins.md).
- **Deferred to FINS v2 (documented, deliberate)**: FINS-over-TCP (a new host over the same transport-agnostic command codec), the expansion (EM) memory banks, the timer/counter areas (present-value + completion-flag with no equivalent in this app's tag model), and `STRING`.
- **Status**: ✅ **COMPLETE — Omron FINS (v1) shipped, real-client E2E-proven, and documented**
